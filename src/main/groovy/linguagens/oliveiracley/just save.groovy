				// analise = syntax.replaceFirst("^[a-zA-Z]","")
				// analise = syntax.replace("${analise}","")
				
				// if(analise){
				// 	map[analise] = properties."lexer.identifier.ident"
				// 	syntax = syntax.replaceFirst("${analise}","")
				// 	continue
				// }

				// analise = syntax.replaceFirst("^[0-9]","")
				// analise = syntax.replace("${analise}","")

				// if(analise){
				// 	map[analise] = properties."lexer.identifier.ident"
				// 	syntax = syntax.replaceFirst("${analise}","")
				// 	continue
				// }



				// println "----------"
				// println syntax


				// break;
				// // ("GROOVY-123: some change" ==~ /[A-Z]{3,6}-\d{1,4}/)
